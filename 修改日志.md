# 2025/11/22
变更概览（核心文件/目录）

  - src/config.py：默认路径切换到 C/C++（SQLite）环境，CodeQL DB 目录改为 data/codeql-dbs-cpp，QL pack 版本说明改为 cpp 版本。
  - src/codeql_queries.py：模板全面改为 C++ 数据流（cpp、semmle.code.cpp.dataflow），YML 依赖改为 codeql/cpp-all，匹配逻辑使用文件路径 + 函数名。
  - src/codeql_vul.py：CodeQL analyze 命令改为 C++ 查询包路径，数据库目录改为 db-cpp。
  - src/iris.py：C++ 原语类型集合、CodeQL DB 路径、qlpack 依赖改为 cpp；固定模块匹配改为 C/C++ 文件；黑名单/包过滤适配不存在包文件时的容错，函数静态判断适
    配字符串类型。
  - src/prompts.py：系统/用户提示重写为 C/C++ 内存安全专家视角，强调 OOB/缓冲区溢出、Guard 检查；POSTHOC 提示加入长度/边界检查要求。
  - src/queries.py：移除 Java CWE 列表，新增 cwe-120wLLM-cpp 与 cwe-787wLLM-cpp 配置，指向新的 C++ 查询，并提供示例函数（文件路径+Global）。
  - 新增 C++ CWE 查询：
      - src/cwe-queries/cwe-120-cpp/{cwe-120wLLM.ql, MyBufferOverflowFlow.qll}
      - src/cwe-queries/cwe-787-cpp/{cwe-787wLLM.ql, MyBufferOverflowFlow.qll}
  - 重写/适配所有辅助 QL 查询为 C++ 扁平结构（文件路径作为 package，类为 Global）：
      - src/queries/fetch_external_apis.ql、fetch_func_params.ql、fetch_func_locs.ql、fetch_class_locs.ql、fetch_sources.ql、fetch_sinks.ql、
        fetch_field_reads.ql、getpackages.ql
  - AGENTS.md：新增贡献指南文档。

  后续建议

  - 用 C++ 版 CodeQL 重新创建 SQLite 的数据库目录 .../db-cpp，再运行 --query cwe-120wLLM-cpp/cwe-787wLLM-cpp 验证 QL 兼容性。
  - 若 CodeQL 版本的 FunctionCall/参数 API 有差异，需按实际 schema 微调匹配代码与查询。

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 已完成的改造（均已修改）：

  - src/config.py：将数据库默认目录改为 C/C++ (data/codeql-dbs-cpp)，源码指向 SQLite 目录，CodeQL 查询版本注释改为 cpp 版本。
  - src/codeql_queries.py：所有模板改用 C++ dataflow（cpp、semmle.code.cpp.dataflow），QLpack 依赖改为 codeql/cpp-all；源/汇/步骤匹配改为基于文件路径 + 函
    数名的扁平结构。
  - src/codeql_vul.py：CodeQL 分析路径改为 codeql/cpp-queries，数据库路径改为 db-cpp。
  - src/iris.py：原 Java 偏好调整为 C++（原语类型集合、数据库路径、固定模块匹配 .c/.cc/.cpp），自定义 qlpack 依赖改为 C++，包/模块过滤兼容无包的项目，黑名
    单替换为数学等无害函数。
  - src/prompts.py：系统/用户提示改为 C/C++ 内存安全角色，强调长度检查 Guard；POSTHOC 逻辑聚焦 OOB/缓冲区溢出；提示示例与字段描述改为文件路径+Global。
  - src/queries.py：清空旧 Java CWE，新增 C++ 的 cwe-120wLLM-cpp、cwe-787wLLM-cpp 配置，指向新的查询文件并提供 C++ 示例。
  - 新增 C++ 查询包：src/cwe-queries/cwe-120-cpp/ 与 src/cwe-queries/cwe-787-cpp/（MyBufferOverflowFlow.qll、cwe-120/787wLLM.ql），用于缓冲区溢出/OOB 流
    追踪。
  - src/queries/*.ql 全部改为 C++ 语义与扁平 schema：fetch_external_apis.ql、fetch_func_params.ql、fetch_class_locs.ql、fetch_func_locs.ql、
    fetch_sources.ql、fetch_sinks.ql、fetch_field_reads.ql、getpackages.ql。
  - 新增贡献指南：AGENTS.md（Repository Guidelines）。

  现状：未运行测试。后续需用 C++ CodeQL 创建 db-cpp 数据库并验证查询能编译运行。

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 已完成 C/C++（SQLite）缓冲区溢出改造核心工作，主要修改如下：

  - 配置与依赖：src/config.py 改为指向 C/C++ 数据库目录 data/codeql-dbs-cpp，默认源码目录为 SQLite，CodeQL 包版本提示改为 cpp 版；src/iris.py 切换 qlpack
    依赖为 codeql/cpp-*，数据库目录改为 db-cpp，原 Java 模块/包处理改为文件/Global 扁平结构，基础类型列表换成 C 类型。
  - CodeQL 模板与执行：src/codeql_queries.py 全面改为 C++ DataFlow，YML 模型依赖改为 codeql/cpp-all，函数/参数匹配使用文件路径与 FunctionCall；src/
    codeql_vul.py 改用 codeql/cpp-queries 路径和 db-cpp。
  - 提示词与查询注册表：src/prompts.py 重写为 C/C++ 内存安全专家角色，加入边界检查/guard 关注；src/queries.py 清空 Java CWE，新增 CWE-120/787（cpp）条目及
    示例，并指向新的 C++ 查询文件。
  - QL 辅助查询（扁平化模式）：src/queries/fetch_external_apis.ql、fetch_func_params.ql、fetch_class_locs.ql、fetch_func_locs.ql、fetch_sources.ql、
    fetch_sinks.ql、fetch_field_reads.ql、getpackages.ql 等全部改为 C++ 语法，输出文件路径作为 package、class 固定为 Global，调用/函数定位改为 Function/
    FunctionCall API。
  - 新缓冲区溢出查询：新增 src/cwe-queries/cwe-120-cpp/ 与 src/cwe-queries/cwe-787-cpp/，包含通用数据流 qll（MyBufferOverflowFlow）和 CWE-120/787 主查询
    （路径问题定义）。
  - 贡献指南：新增 AGENTS.md，提供仓库结构、命令、风格和安全配置建议（C++/CodeQL 适配）。

  后续建议：用 codeql database create --language=cpp .../db-cpp 为 SQLite 构建 C++ DB，再用 --query cwe-120wLLM-cpp 或 cwe-787wLLM-cpp 运行管道，必要时依
  据本地 CodeQL 版本调整 FunctionCall API/签名。

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 已完成将 IRIS 从 Java 改造为 C/C++（SQLite）缓冲区溢出/OOB 检测的核心改动，主要涉及以下文件：

  - src/config.py：默认路径改为 C/C++ 数据库（data/codeql-dbs-cpp）与 SQLite 源码目录，CodeQL 依赖版本注释改为 cpp 包。
  - src/codeql_queries.py：全部模板切换为 C++ dataflow（import cpp），扩展包改为 codeql/cpp-all，匹配逻辑使用文件路径+函数名（无包/类层级），调整 sink/
    source/summary 生成片段。
  - src/codeql_vul.py：分析命令改用 codeql/cpp-queries，数据库路径改为 db-cpp。
  - src/iris.py：primitive 类型换成 C 类型；数据库检查指向 db-cpp；qlpack 依赖改为 codeql/cpp-*；模块过滤更健壮；固定模块匹配改为 C/C++ 源码；默认不依赖
    Java 包结构。
  - src/prompts.py：系统/用户/后处理提示全面改写为 C/C++ 内存安全语境，强调长度/边界检查（Guard）及 OOB 判定；POSTHOC 提示加入 CWE-120/787 说明。
  - src/queries.py：清空原 Java CWE，新增 C++ 缓冲区溢出配置（cwe-120wLLM-cpp、cwe-787wLLM-cpp）及示例，保留辅助查询条目。
  - 新增 C++ 查询模板：src/cwe-queries/cwe-120-cpp/*、src/cwe-queries/cwe-787-cpp/*（MyBufferOverflowFlow.qll + 主 .ql）。
  - src/queries/fetch_*.ql 系列：全部从 Java 改为 C++，采用扁平 schema（文件路径=package，class=“Global”），重写函数签名/参数收集、source/sink 提取、外部
    API 抽取等。
  - AGENTS.md：添加贡献指南（构建、脚本、风格、测试、PR 规范等）。

  后续建议：用 codeql database create --language=cpp …/db-cpp 针对 SQLite 源码生成数据库，再运行 --query cwe-120wLLM-cpp 或 cwe-787wLLM-cpp 验证；如
  CodeQL 版本 API 有差异，检查 FunctionCall/参数 API。

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 主要改动概览（中文）：

  - src/config.py：默认路径切换到 C/C++（SQLite）环境，CodeQL 库/数据库目录改为 codeql/cpp-* 和 db-cpp，项目源码定位到 SQLite 目录。
  - src/codeql_queries.py：数据流模板改用 C++ 的 semmle.code.cpp.dataflow，源/汇/步长匹配重写为文件路径+函数名的扁平模型，扩展包依赖改为 codeql/cpp-all。
  - src/iris.py：管道适配 C++（primitive 类型、db-cpp 路径、qlpack 依赖），处理模块文件与函数的扁平结构，黑名单与参数判定逻辑更新。
  - src/codeql_vul.py：CodeQL 分析命令改为 C++ 查询包路径与 db-cpp。
  - src/prompts.py：系统/用户/后处理提示重写为 C/C++ 内存安全专家语境，强调 OOB 守卫检查和缓冲区写入场景。
  - src/queries.py：清空 Java CWE，新增 C++ 的 cwe-120wLLM-cpp 和 cwe-787wLLM-cpp 配置与示例。
  - src/queries/*.ql（fetch_external_apis, fetch_func_params, fetch_class_locs, fetch_func_locs, fetch_sources, fetch_sinks, fetch_field_reads,
    getpackages）：全部重写为 C++ 语法与扁平 file/Global 结构，收集函数调用、参数、位置等信息。
  - 新增 src/cwe-queries/cwe-120-cpp/ 与 src/cwe-queries/cwe-787-cpp/：包含缓冲区溢出/OOB 的 LLM 入口查询和数据流配置 (MyBufferOverflowFlow.qll, cwe-
    120wLLM.ql, cwe-787wLLM.ql)。
  - AGENTS.md：新增仓库贡献指南（简明版）。

  未运行测试；后续需用 CodeQL C/C++ 生成 SQLite 的 db-cpp 并试跑 cwe-120wLLM-cpp / cwe-787wLLM-cpp。

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• - 配置切换到 C/C++：src/config.py 设定 CodeQL DB 路径为 data/codeql-dbs-cpp、源码目录指向 SQLite、查询版本指向 codeql/cpp-queries。src/iris.py 和 src/
    codeql_vul.py 改为使用 C++ CodeQL 数据库/qlpack 依赖，基础类型改为 C 系，模块过滤改为扁平文件/函数结构。
  - CodeQL 模板与提取查询：src/codeql_queries.py 使用 C++ dataflow，匹配函数/文件路径；src/queries/fetch_*.ql、getpackages.ql 全部改写为 C++ 版，schema 扁
    平为 file→package、Global→class。
  - 提示词与查询注册：src/prompts.py 重写为 C/C++ 内存安全语境，强调 OOB/拷贝越界及守卫检查；src/queries.py 清空原 Java CWE，新增 C/C++ 缓冲区溢出/越界
    (cwe-120wLLM-cpp, cwe-787wLLM-cpp) 配置。
  - 新的 C++ CodeQL 规则：新增 src/cwe-queries/cwe-120-cpp/ 与 src/cwe-queries/cwe-787-cpp/，包含数据流配置 MyBufferOverflowFlow.qll 和检测查询 cwe-
    120wLLM.ql、cwe-787wLLM.ql。
  - 其他：AGENTS.md 增加仓库贡献指南（结构说明、命令、风格、测试、PR 要求）。

# 2025/11/23 18:00
改动
  - 更新 src/queries/fetch_class_locs.ql、fetch_func_locs.ql、fetch_func_params.ql、fetch_field_reads.ql、fetch_external_apis.ql，移除
    Java 的 package/clazz 概念，改用 C++ API（如 getType()、getQualifiedName()、getNumberOfParameters()），输出 file_path 等 C++ 语义
    字段。
  - 新增 src/queries/MySources.qll、src/queries/MySinks.qll，定义 C 风格输入函数（scanf/gets/fgets）为源，memcpy/strcpy/strncpy/memmove
    目标参数为汇，供 fetch_sources.ql、fetch_sinks.ql 使用。
  - 函数/类查询改为使用限定名，避免重载和命名空间混淆，并实际枚举 C++ 类/结构位置。